# Third-party packages this project needs. The gateway itself (TCP/UDP
# servers, protocol, config, the cifX ctypes bindings) uses only the
# Python standard library - nothing here is required just to run
# `cfix-gateway`.
#
# PySide6 is only needed for the desktop GUI (cfix-gateway-gui).
PySide6>=6.5

# pytest is only needed to run the test suite (tests/). Not required to
# run the gateway or the GUI.
pytest>=7.0
